# Digest

```
Read pulse/skills/digest/SKILL.md in this repo and follow every step in it exactly, for yesterday in $PULSE_TZ. Do not push if any gate fails or the reviewer subagent returns FAIL. Mark rows processed only after the push is verified.
```

Environment: `pulse`
Trigger: cron `0 5 * * *` (in `PULSE_TZ`)
